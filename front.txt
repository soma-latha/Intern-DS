🌐 1️⃣ Form Validation (HTML + JS)
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Form Validation</title>
  <style>
    body { font-family: Arial; margin: 40px; }
    label { display:block; margin-top:10px; }
    input { padding:8px; width:250px; }
    small { color:red; }
    button { margin-top:15px; padding:8px 12px; }
  </style>
</head>
<body>
  <h2>Form Validation</h2>
  <form id="myForm">
    <label>Name</label>
    <input type="text" id="name">
    <small id="nameErr"></small>

    <label>Email</label>
    <input type="email" id="email">
    <small id="emailErr"></small>

    <label>Password</label>
    <input type="password" id="password">
    <small id="passErr"></small><br>

    <button type="submit">Submit</button>
  </form>
  <pre id="result"></pre>

  <script>
    const form = document.getElementById('myForm');
    const result = document.getElementById('result');

    form.addEventListener('submit', (e) => {
      e.preventDefault();
      let name = form.name.value.trim();
      let email = form.email.value.trim();
      let password = form.password.value.trim();

      let valid = true;
      document.querySelectorAll('small').forEach(s => s.textContent = '');

      if(name.length < 3){
        document.getElementById('nameErr').textContent = 'Name must be at least 3 chars';
        valid = false;
      }
      if(!email.match(/^\\S+@\\S+\\.\\S+$/)){
        document.getElementById('emailErr').textContent = 'Invalid email';
        valid = false;
      }
      if(password.length < 6){
        document.getElementById('passErr').textContent = 'Password must be at least 6 chars';
        valid = false;
      }

      if(valid){
        result.textContent = JSON.stringify({ name, email, password }, null, 2);
        form.reset();
      }
    });
  </script>
</body>
</html>

⚖️ 2️⃣ BMI Calculator
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>BMI Calculator</title>
  <style>
    body { font-family: Arial; margin: 40px; }
    input { padding:6px; width:100px; }
  </style>
</head>
<body>
  <h2>BMI Calculator</h2>
  <label>Height (cm): </label>
  <input type="number" id="height" value="170"><br><br>
  <label>Weight (kg): </label>
  <input type="number" id="weight" value="65"><br><br>
  <button onclick="calcBMI()">Calculate</button>

  <h3 id="bmi"></h3>
  <h4 id="category"></h4>

  <script>
    function calcBMI(){
      const h = parseFloat(document.getElementById('height').value) / 100;
      const w = parseFloat(document.getElementById('weight').value);
      if(!h || !w) return;
      const bmi = (w / (h*h)).toFixed(2);
      document.getElementById('bmi').textContent = `Your BMI: ${bmi}`;
      let cat = '';
      if(bmi < 18.5) cat = 'Underweight';
      else if(bmi < 25) cat = 'Normal';
      else if(bmi < 30) cat = 'Overweight';
      else cat = 'Obese';
      document.getElementById('category').textContent = `Category: ${cat}`;
    }
  </script>
</body>
</html>

📝 3️⃣ Simple CRUD Application (LocalStorage)
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Simple CRUD</title>
  <style>
    body { font-family: Arial; margin: 40px; }
    input { padding:8px; width:250px; }
    button { padding:8px 10px; margin-left:5px; }
  </style>
</head>
<body>
  <h2>Simple CRUD (LocalStorage)</h2>
  <input type="text" id="itemInput" placeholder="Enter item">
  <button onclick="addItem()">Add</button>
  <ul id="itemList"></ul>

  <script>
    const list = document.getElementById('itemList');
    let items = JSON.parse(localStorage.getItem('items') || '[]');
    render();

    function addItem(){
      const input = document.getElementById('itemInput');
      const title = input.value.trim();
      if(!title) return;
      items.push({ id: Date.now(), title });
      localStorage.setItem('items', JSON.stringify(items));
      input.value = '';
      render();
    }

    function deleteItem(id){
      items = items.filter(i => i.id !== id);
      localStorage.setItem('items', JSON.stringify(items));
      render();
    }

    function render(){
      list.innerHTML = '';
      if(items.length === 0) list.innerHTML = '<li>No items added yet</li>';
      items.forEach(i => {
        const li = document.createElement('li');
        li.textContent = i.title;
        const del = document.createElement('button');
        del.textContent = 'Delete';
        del.onclick = () => deleteItem(i.id);
        li.appendChild(del);
        list.appendChild(li);
      });
    }
  </script>
</body>
</html>

🌍 4️⃣ Fetch Data from API (JSONPlaceholder)
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Fetch API Example</title>
  <style>
    body { font-family: Arial; margin: 40px; }
    li { margin-bottom: 8px; }
  </style>
</head>
<body>
  <h2>Fetch API Example</h2>
  <label>Number of posts: </label>
  <input type="number" id="limit" value="5">
  <button onclick="fetchPosts()">Fetch</button>
  <ul id="posts"></ul>

  <script>
    async function fetchPosts(){
      const limit = document.getElementById('limit').value;
      const ul = document.getElementById('posts');
      ul.innerHTML = '<li>Loading...</li>';
      try {
        const res = await fetch(`https://jsonplaceholder.typicode.com/posts?_limit=${limit}`);
        const data = await res.json();
        ul.innerHTML = '';
        data.forEach(p => {
          const li = document.createElement('li');
          li.innerHTML = `<strong>${p.title}</strong><br>${p.body}`;
          ul.appendChild(li);
        });
      } catch (err) {
        ul.innerHTML = `<li style='color:red;'>Error: ${err}</li>`;
      }
    }
  </script>
</body>
</html>

mkdir latha-app
cd latha-app
code .
docker build -t my-image:1.0 .
docker run my-image:1.0
docker images
docker login
docker tag my-image:1.0 somalatha/my-image:1.0
docker push somalatha/my-image:1.0
docker pull somalatha/my-image:1.0
docker run somalatha/my-image:1.0




C:\Users\latha\OneDrive\Desktop\final project\devops
│
├── server.js
└── index.html


frontend 

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BMI Calculator</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f3f8;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 300px;
      text-align: center;
    }
    h2 {
      color: #333;
    }
    input {
      width: 90%;
      padding: 8px;
      margin: 8px 0;
      border: 1px solid #aaa;
      border-radius: 6px;
    }
    button {
      background-color: #007bff;
      color: white;
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0056b3;
    }
    .result {
      margin-top: 10px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>BMI Calculator</h2>
    <input type="number" id="weight" placeholder="Enter weight (kg)">
    <input type="number" id="height" placeholder="Enter height (m)">
    <br>
    <button onclick="calculateBMI()">Calculate</button>
    <button onclick="clearFields()">Clear</button>
    <div class="result" id="result"></div>
  </div>

  <script>
    function calculateBMI() {
      const weight = parseFloat(document.getElementById("weight").value);
      const height = parseFloat(document.getElementById("height").value);
      const result = document.getElementById("result");

      if (!weight || !height || height <= 0) {
        result.textContent = "Please enter valid numbers!";
        result.style.color = "red";
        return;
      }

      const bmi = weight / (height * height);
      let category = "";

      if (bmi < 18.5) category = "Underweight";
      else if (bmi < 24.9) category = "Normal weight";
      else if (bmi < 29.9) category = "Overweight";
      else category = "Obese";

      result.textContent = `Your BMI is ${bmi.toFixed(2)} (${category})`;
      result.style.color = "#333";
    }

    function clearFields() {
      document.getElementById("weight").value = "";
      document.getElementById("height").value = "";
      document.getElementById("result").textContent = "";
    }
  </script>
</body>
</html>     



backend 

// server.js
const express = require('express');
const path = require('path');
const app = express();

// Serve static files like HTML, CSS, JS
app.use(express.static(__dirname));

// Start server
app.listen(3000, () => {
  console.log('BMI app running on http://localhost:5500');



cmd 

npm init -y
npm install express
node server.js


before colour

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Before Color Page</title>
</head>
<body style="text-align:center; margin-top:150px;">
  <h1>Before Colour Page 🎨</h1>
  <p>Click below to see the colorful page!</p>
  <a href="after.html" style="font-size:20px; text-decoration:none; color:blue;">Go to Colorful Page</a>
</body>
</html>



after coloyr 

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>After Color Page</title>
  <style>
    body {
      background: linear-gradient(to right, #00f260, #0575e6);
      font-family: Arial, sans-serif;
      color: white;
      text-align: center;
      margin-top: 150px;
    }
    h1 {
      font-size: 40px;
      margin-bottom: 30px;
    }
    input[type="text"] {
      padding: 10px;
      width: 250px;
      border-radius: 10px;
      border: none;
      font-size: 18px;
    }
    button {
      background-color: #ff0080;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 10px;
      font-size: 18px;
      margin-left: 10px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>🌈 Colorful Page 🌈</h1>
  <input type="text" id="userInput" placeholder="Enter something...">
  <button onclick="showMessage()">Submit</button>

  <script>
    function showMessage() {
      let val = document.getElementById('userInput').value;
      if (val)
        alert("You entered: " + val);
      else
        alert("Please enter something first!");
    }
  </script>
</body>
</html>


















